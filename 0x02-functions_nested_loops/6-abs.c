#include "main.h"

/**
  * _abs - C function to compute the absolute value of an integer
  *
  *@n: number
  *
  * Return: the absolute value
  */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
}
